dependencies {
	testImplementation('com.netflix.nebula:nebula-test:11.0.0') {
        exclude group: 'org.codehaus.groovy'
  }
}

test {
    useJUnitPlatform()

    testLogging {
        events "started", "passed", "skipped", "failed"
    }
}
