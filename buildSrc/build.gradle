dependencies {
	testImplementation('com.netflix.nebula:nebula-test:11.1.5') {
        exclude group: 'org.codehaus.groovy'
  }
}

test {
    useJUnitPlatform()

    testLogging {
        events "started", "passed", "skipped", "failed"
    }
}
